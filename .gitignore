# --- secrets / env (keep only the template) ---
.env
*.env
!.env.example

# --- internal design docs (not for public repo) ---
整章化流水线改造设计.md

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# --- tooling / ide ---
.idea/
.vscode/
.codebuddy/

# --- os ---
Thumbs.db
Desktop.ini
.DS_Store

# --- heavy / regenerable data ---
# deeplearning/ is fully tracked: the source PDF & MinerU outputs are from the open-licensed nndl-v2 book.
# Other *.pdf anywhere else stay ignored by default (rule below).
*.pdf
!deeplearning/**/*.pdf
